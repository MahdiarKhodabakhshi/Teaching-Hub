#include <stdio.h>

int main() {
    char ch;
    printf("Enter a character: ");
    ch = getchar();  // Reads one character from standard input
    printf("You entered: %c\n", ch);
    return 0;
}
