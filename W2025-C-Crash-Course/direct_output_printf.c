#include <stdio.h>

int main() {
    char name[] = "Alice";
    int age = 20;

    //# Directly prints to the screen
    printf("Name: %s, Age: %d\n", name, age);//# Direct output to stdout
    return 0;
}
